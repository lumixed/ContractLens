# Baseline

Frontier model (Claude/GPT-4) baseline evaluation for comparison against the fine-tuned model.

## Contents

- `run_baseline.py` — Script to run few-shot prompted classification on the test set
- `baseline_results.json` — Per-category precision/recall/F1 and cost/latency metrics

*Files will be added in Steps 7–8.*
